"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "command -v bun >/dev/null && command -v jq >/dev/null && command -v tmux >/dev/null && command -v python3 >/dev/null"
run = "bash tests/run.sh"
run_mode = "concurrent"
archive = "true"
